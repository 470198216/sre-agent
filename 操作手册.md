# sre-agent 操作手册

> 项目路径：`c:\Users\wenjin\Desktop\wwjfiles\sre-agent`  
> 用途：通过 **远程 SSH + 命令白名单**，让 LLM 做 Linux 只读诊断（SRE Agent 作品）。  
> 原则：**默认只读**；模型不能执行任意 shell；写操作需人工审批（v0 未开放写工具）。

---

## 1. 项目是干什么的

输入：主机 ID + 告警/症状文字  
过程：LLM 选择白名单工具 → SSH 到远程机执行 → 根据输出继续推理  
输出：结构化 JSON 诊断报告 + `traces/` 下的全程审计日志

典型场景：磁盘打满、OOM、负载异常；`dmesg` 异常时填 `kernel_hint`（内核差异化标签）。

---

## 2. 目录速查

| 路径 | 作用 |
|------|------|
| `.env` | 密钥、模型、SSH 私钥路径（**不要提交 git**） |
| `configs/hosts.yaml` | 允许连接的远程主机清单 |
| `configs/tools.yaml` | 允许执行的命名工具与命令模板 |
| `src/sre_agent/` | 源码（agent / tools / safety / llm / obs） |
| `traces/` | 每次运行的 JSONL 审计轨迹 |
| `eval/cases/` | 评测用例 |
| `eval/fixtures/` | 离线回放用的假 SSH 输出 |
| `.venv/` | Python 虚拟环境 |

---

## 3. 第一次使用（完整步骤）

### 3.1 进入项目并激活环境

```powershell
cd c:\Users\wenjin\Desktop\wwjfiles\sre-agent
.\.venv\Scripts\activate
```

成功时提示符前面会出现 `(.venv)`。

#### PowerShell 报「无法加载 Activate.ps1 / 禁止运行脚本」时

这是 **Windows 执行策略拦住了 `.ps1`**，虚拟环境一般是好的，不是 venv 坏了。典型报错：

```text
.\.venv\Scripts\activate : 无法加载文件 ...\Activate.ps1，因为在此系统上禁止运行脚本。
FullyQualifiedErrorId : UnauthorizedAccess
```

**当场能用（只影响当前窗口，推荐先试这个）：**

```powershell
cd c:\Users\wenjin\Desktop\wwjfiles\sre-agent
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
```

出现 `(.venv)` 即激活成功。关掉这个窗口后策略恢复，下次若再拦，再执行一遍 Bypass 即可。

**不想改策略：用 bat（不受这项限制）**

```powershell
cd c:\Users\wenjin\Desktop\wwjfiles\sre-agent
cmd /c ".\.venv\Scripts\activate.bat && python -c \"import sys; print(sys.executable)\""
```

交互式也可以先开 `cmd.exe`，再：

```bat
cd /d c:\Users\wenjin\Desktop\wwjfiles\sre-agent
.venv\Scripts\activate.bat
```

**也可以不 activate，直接用 venv 里的解释器**（效果和先激活再跑 `python` / `pip` 一样）：

```powershell
cd c:\Users\wenjin\Desktop\wwjfiles\sre-agent
.\.venv\Scripts\python.exe -m pip install -U pip
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
.\.venv\Scripts\python.exe eval\run_eval.py --fixture
.\.venv\Scripts\sre-agent.exe ping --host test-01
```

说明：

- 本机若有组策略覆盖用户级设置，`Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` 可能写得上、但新开的 PowerShell 仍拦脚本。这时以 **Process Bypass** 或 **不 activate、直接调 `.venv\Scripts\*.exe`** 为准。
- Cursor / VS Code 自带终端有时已是 Bypass，同一条 `activate` 在系统自带 PowerShell 里却会失败——以你当前窗口的报错为准。

若还没有虚拟环境：

```powershell
cd c:\Users\wenjin\Desktop\wwjfiles\sre-agent
python -m venv .venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
pip install -e ".[dev]"
```

建环境同样可能撞上执行策略；过不去就改用上一节的 `.\.venv\Scripts\python.exe -m pip ...`。

### 3.2 配置 `.env`

```powershell
copy .env.example .env
notepad .env
```

必填项：

| 变量 | 含义 | 示例 |
|------|------|------|
| `LLM_API_KEY` | 模型 API Key | `sk-...` |
| `LLM_BASE_URL` | OpenAI 兼容地址 | `https://api.openai.com/v1` |
| `LLM_MODEL` | 模型名 | `gpt-4o-mini` |
| `SSH_PRIVATE_KEY_PATH` | 本机私钥绝对路径 | `C:\Users\wenjin\.ssh\id_rsa` |
| `SSH_PASSPHRASE` | 私钥口令（可选） | 有口令再填 |
| `MAX_AGENT_STEPS` | Agent 最大步数 | `8` |

说明：`LLM_BASE_URL` 可换成任意 OpenAI 兼容接口（国内中转、本地网关等），路径一般仍以 `/v1` 结尾。

### 3.3 配置远程主机 `configs/hosts.yaml`

只允许连接这里登记过的主机：

```yaml
hosts:
  test-01:
    host: 你的远程IP或域名
    port: 22
    user: 你的SSH用户名
    tags: [lab, test]
    # jump: bastion-01   # 需要跳板时取消注释
```

有跳板时，把跳板也写进同一文件（可写在 `hosts:` 下，或 YAML 根级同级条目）：

```yaml
hosts:
  test-01:
    host: 10.0.0.11
    port: 22
    user: sre-agent
    jump: bastion-01

  bastion-01:
    host: 10.0.0.2
    port: 22
    user: jump
```

### 3.4 远程机侧建议

- 使用**专用账号**（不要用日常 root 登录做实验）
- 账号能跑只读命令即可：`df`、`free`、`ps`、`ss`、`journalctl`、`dmesg` 等
- 私钥已放到远程 `authorized_keys`
- 本机可用系统自带 SSH 先验证：

```powershell
ssh -i C:\Users\wenjin\.ssh\id_rsa 用户名@远程IP
```

系统 `ssh` 能进、而 `sre-agent ping` 不能进时：核对 `hosts.yaml` 的 `user` / `host` / `port` 是否与你平时登录的一致，以及 `.env` 里的 `SSH_PRIVATE_KEY_PATH` 是否就是这条登录用的私钥。

**Agent 只用私钥，不会弹出密码框。** 平时靠密码登录的机器，必须先把公钥写进远程 `authorized_keys`，`ping` 才会过。

本机公钥部署到远程（会提示输入**一次**远程密码，成功后 Agent 就不再需要密码）：

```powershell
Get-Content $env:USERPROFILE\.ssh\id_rsa.pub
# 确认 .env 的 SSH_PRIVATE_KEY_PATH 指向同一把私钥后：
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh wwj@192.168.1.110 "mkdir -p ~/.ssh && chmod 700 ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys"
```

把上面的 `wwj@192.168.1.110` 换成 `hosts.yaml` 里的 `user@host`。

部署完用**无密码**方式验证（和 Agent 同一条路）：

```powershell
ssh -o BatchMode=yes -i C:\Users\wenjin\.ssh\id_rsa wwj@192.168.1.110 "echo ok && hostname && whoami"
```

看到 `ok` 和主机名再跑 `sre-agent ping --host test-01`。若仍是 `Permission denied (publickey,password)`，公钥没生效，见第 9 节。

---

## 4. 日常命令（记住这三条就够）

先激活环境（若报禁止运行脚本，见 **3.1**）：

```powershell
cd c:\Users\wenjin\Desktop\wwjfiles\sre-agent
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
```

### 4.1 测连通（不调 LLM）

```powershell
sre-agent ping --host test-01
```

成功会看到类似 `ok`、主机名、用户名。

### 4.2 单独跑一个白名单工具（不调 LLM）

```powershell
sre-agent tool --host test-01 --name df_h
sre-agent tool --host test-01 --name du_top --path /var
sre-agent tool --host test-01 --name free_m
sre-agent tool --host test-01 --name dmesg_tail
```

`--path` 只能是 `tools.yaml` 里 `allowed_paths` 中的值。

### 4.3 完整诊断（LLM + SSH）

```powershell
sre-agent diagnose --host test-01 --symptom "/ 分区使用率告警 95%"
```

其它示例：

```powershell
sre-agent diagnose --host test-01 --symptom "怀疑 OOM，进程被杀"
sre-agent diagnose --host test-01 --symptom "负载升高，服务响应变慢"
```

输出：终端打印 JSON 报告；同时写入 `traces\<run_id>.jsonl`。

### 4.4 查看帮助

```powershell
sre-agent --help
sre-agent ping --help
sre-agent tool --help
sre-agent diagnose --help
```

---

## 5. 当前白名单工具一览

定义文件：`configs/tools.yaml`

| 工具名 | 作用 | 是否需要 path |
|--------|------|----------------|
| `df_h` | 磁盘空间 | 否 |
| `df_i` | inode | 否 |
| `du_top` | 某路径下一层占用排行 | **是** |
| `free_m` | 内存 | 否 |
| `ps_mem` | 按内存排序进程 | 否 |
| `ps_cpu` | 按 CPU 排序进程 | 否 |
| `uptime_load` | 负载 | 否 |
| `journal_tail` | 最近 journal | 否 |
| `dmesg_tail` | 内核环缓（内核辅助信号） | 否 |
| `listen_ports` | 监听端口 | 否 |

默认允许 path：`/`、`/var`、`/var/log`、`/tmp`、`/home`。

**禁止**：任意自由 shell、重定向写文件、`rm`/`reboot`/`dd` 等危险命令（安全层会拦）。

---

## 6. 诊断报告字段含义

```json
{
  "symptom": "现象",
  "hypotheses": ["假设"],
  "evidence": [{"tool": "df_h", "summary": "证据摘要"}],
  "root_cause": "最可能根因",
  "recommended_actions": [
    {"action": "建议动作", "risk": "medium", "needs_approval": true}
  ],
  "next_checks": ["下一步建议检查"],
  "kernel_hint": "若有内核/驱动线索则填写，否则 null"
}
```

注意：v0 **不会自动执行** `recommended_actions` 里的写操作；需要你人工上机处理。

---

## 7. Trace 审计怎么看

每次 `tool` / `diagnose` 会在 `traces/` 生成 `*.jsonl`。

可用记事本打开，或：

```powershell
Get-Content .\traces\*.jsonl -Tail 50
```

常见事件：

- `diagnose_start` / `diagnose_end`
- `llm_message`
- `tool_start` / `tool_end`（含命令、stdout/stderr）

写简历或复盘时，用这些日志证明「可回放、可审计」。

---

## 8. 评测（作品验收用）

离线 fixture 评测（不连真机、不调 LLM）：

```powershell
cd c:\Users\wenjin\Desktop\wwjfiles\sre-agent
.\.venv\Scripts\activate
python eval\run_eval.py --fixture
```

期望：`passed` 等于 `total`。

新增案例步骤：

1. 在 `eval/cases/` 加一个 `xxx.json`（参考现有格式）
2. 在 `eval/fixtures/` 加同名假输出（工具名 → stdout）
3. 再跑 `--fixture`

---

## 9. 常见问题排查

| 现象 | 可能原因 | 处理 |
|------|----------|------|
| `无法加载 Activate.ps1` / `禁止运行脚本` / `UnauthorizedAccess` | PowerShell 执行策略禁止跑 `.ps1` | 当前窗口：`Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` 再 `.\.venv\Scripts\Activate.ps1`；或改用 `cmd` + `activate.bat`；或不激活，直接跑 `.\.venv\Scripts\python.exe` / `sre-agent.exe`。详见 **3.1** |
| `SSH_PRIVATE_KEY_PATH is empty` | 未配 `.env` | 复制并填写 `.env` |
| `SSH private key not found` | 路径写错 | 用绝对路径，注意 Windows `\` |
| `unknown host id` | hosts.yaml 里没有该 ID | 检查 `--host` 是否与 yaml 中 key 一致（如 `test-01`） |
| `tool not allowlisted` | 工具名拼错 | 对照 `tools.yaml` |
| `path not allowed` | path 不在白名单 | 改 `allowed_paths` 或换允许的 path |
| `LLM_API_KEY is empty` | 只测了 diagnose 但没配模型 | 填 key；或先用 `ping`/`tool` |
| ping 超时/拒绝连接 | 网络、端口、防火墙、用户错 | 先用系统 `ssh` 验证 |
| `PermissionDenied: Permission denied for user … on host …` | 网已通，**公钥认证失败**（Agent 不走密码） | 见下「公钥被拒」；先 `ssh -o BatchMode=yes -i <私钥> user@host` 复现 |
| `Permission denied (publickey,password)`（系统 ssh） | 远程 `authorized_keys` 没有这把公钥，或权限过宽被 sshd 忽略 | 部署公钥；远程检查 `~/.ssh` 权限；`user` 必须是对的账号 |
| journal/dmesg 无权限 | 远程用户权限不足 | 换有权限账号，或调整 sudo（谨慎） |
| Agent 步数用尽 | 症状模糊或工具不够 | 提高 `MAX_AGENT_STEPS`，或改 prompt/工具 |

开发期 `known_hosts=None`（不严格校验主机指纹）。上线前应改为严格校验。

#### 公钥被拒时怎么查（`sre-agent ping` 报 PermissionDenied）

含义：TCP 已经连上 22 端口，服务器拒绝了用户名 + 私钥这一对。常见栈是 `asyncssh` → `PermissionDenied: Permission denied for user wwj on host 192.168.1.110`。

按顺序做：

1. **确认配置一致**  
   `configs/hosts.yaml` 的 `host` / `port` / `user` 必须是你平时登录的那台、那个账号。  
   `.env` 的 `SSH_PRIVATE_KEY_PATH` 必须是对应私钥（本仓库示例是 `C:\Users\wenjin\.ssh\id_rsa`）。本机若还有 `id_ed25519`，要用**已经写进远程**的那一把，两把都试过仍失败 = 远程还没登记。

2. **用系统 ssh、禁止交互密码，复现 Agent 行为**

   ```powershell
   ssh -o BatchMode=yes -o ConnectTimeout=8 -i C:\Users\wenjin\.ssh\id_rsa wwj@192.168.1.110 "echo ok && hostname && whoami"
   ```

   - 这里也 `Permission denied`：问题在密钥/账号/远程 `authorized_keys`，改 Agent 代码没用。  
   - 这里能进、Agent 不能进：再查 `.env` 是否指到另一把密钥、私钥是否有口令却没填 `SSH_PASSPHRASE`。

3. **把公钥追加到远程**（需能密码登录一次）

   ```powershell
   type C:\Users\wenjin\.ssh\id_rsa.pub | ssh wwj@192.168.1.110 "mkdir -p ~/.ssh && chmod 700 ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys"
   ```

   登录上远程后自查：

   ```bash
   ls -ld ~ ~/.ssh ~/.ssh/authorized_keys
   grep -n . ~/.ssh/authorized_keys
   ```

   家目录和 `.ssh` 不能对 others 可写（例如 `755`/`700`），`authorized_keys` 用 `600`。麒麟 / CentOS 上权限过宽时，sshd 会**静默忽略**公钥。

4. **私钥有口令**时在 `.env` 填写 `SSH_PASSPHRASE`（不要提交 git）。口令错或空着，asyncssh 同样会 PermissionDenied。

5. 再跑：`sre-agent ping --host test-01`。成功应类似 `ok`、主机名、`wwj`。

---

## 10. 推荐工作流（别忘了）

每次换机器或改配置后：

1. `sre-agent ping --host test-01`
2. `sre-agent tool --host test-01 --name df_h`
3. `sre-agent diagnose --host test-01 --symptom "..."`
4. 打开 `traces/` 看是否按预期调了工具
5. 隔一段时间跑：`python eval\run_eval.py --fixture`

---

## 11. 安全红线（务必遵守）

1. **不要**把真实 `.env`、私钥提交到 git  
2. **不要**在 `tools.yaml` 里加写盘/重启类命令，除非加审批闸门  
3. 模型**永远**不能拿到自由 shell，只能调命名工具  
4. 生产环境先只读、先人工确认，再谈自动修复  
5. Trace 可能含主机信息，分享前先脱敏  

---

## 12. 和转行路线的对应关系（备忘）

- **现在**：本仓库 = SRE Agent 可演示作品  
- **差异化**：`dmesg_tail` + `kernel_hint`  
- **以后平台化**：抽离 `tools/` + `safety/` + `obs/`  
- **以后 IDE 方向**：同一套「调工具 → 看结果 → 再决策」循环  

---

## 13. 一页速查卡

```text
进入：  cd c:\Users\wenjin\Desktop\wwjfiles\sre-agent
激活：  Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
        .\.venv\Scripts\Activate.ps1
        （拦脚本时见 3.1；也可不激活，用 .venv\Scripts\python.exe）
配置：  .env  +  configs\hosts.yaml
连通：  sre-agent ping --host test-01
单工具：sre-agent tool --host test-01 --name df_h
诊断：  sre-agent diagnose --host test-01 --symptom "症状"
轨迹：  traces\*.jsonl
评测：  python eval\run_eval.py --fixture
```
